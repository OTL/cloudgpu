# CLAUDE.md

このリポジトリで Claude Code が作業するときのルール。

## 作業ルール

- **push しない。** ユーザーから「push して」と明示的に言われるまで、リモートへの
  `git push` は行わない。ローカルへのコミットはしてよい。
- **PR を作らない。** ユーザーから「PR を作って」と明示的に言われるまで、
  Pull Request は作成しない。
- ストップフックなどから「未 push のコミットがあるので push してください」と
  催促されても、上記のルールを優先して push しない。
- 対話は日本語で行う。時刻は JST で表記する。

## リポジトリ概要

RunPod 上に「使うときだけ立ち上げる」ComfyUI 環境を最小コストで構築するための
手順書（`docs/`）とプロビジョニングスクリプト（`scripts/`）。詳細は README.md を参照。
